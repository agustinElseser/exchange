import styled from "styled-components";

export const Form = styled.form`
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  padding: 0;
  gap: 1rem;
`;

export const LabelFormHelp = styled.div`
  color: var(--color-sell);
  font-size: 0.7rem;
  margin-top: 5px;
`;
